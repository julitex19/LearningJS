const number = [8, 10, 18, 20];

// Array Methods
// unshift methods adds new item at the beginning of the array
// unshift mutates or changes the array

console.log(number)

number.unshift(25)

console.log(number)

