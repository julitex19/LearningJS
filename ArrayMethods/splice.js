const number = [8, 10, 18, 20];

// Array Methods
// splice is used to remove items in an array;
// it mutates or changes the array;
// it takes two parameters, the starting index, and the number of items to be removed;

console.log(number)

const removedItem = number.splice(1, 1)

console.log(removedItem, number)

