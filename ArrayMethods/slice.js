const number = [8, 10, 18, 20];

// Array Methods
// slice method cuts a portion of an array;
// slice does not mutates or changes an array but returns a new array
// slice takes 2 parameter; start and end -> index

console.log(number)

const newNumberArray = number.slice(1, 3)

console.log(newNumberArray)

