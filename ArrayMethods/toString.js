const number = [1,2,3]

const convertToString = number.toString()

// Array Methods
// toString method changes an array to string;

// console.log(Array.isArray(number))

console.log(typeof convertToString);